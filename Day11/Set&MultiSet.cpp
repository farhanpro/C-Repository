#include<iostream>
#include <set>
#include <algorithm>
using namespace std;

int main()
{
    set<int> myset = {12,13,14,15,16};
    myset.insert(2);
    myset.insert(3);
    myset.insert(4);
    myset.insert(2);
    while(!myset.empty())
    {

    }
    return 0;
}
